# Optional. The app calls the API at /api/helpdesk on its own origin by default.
# VITE_HELPDESK_API=/api/helpdesk

# Where `npm run dev` forwards /api/helpdesk (read by vite.config.js, not the browser).
# HELPDESK_API_TARGET=http://localhost:8000
